# bootstrap-run

A minimal governed execution skill for early Polaris cluster sessions (Clusters 2–5). This temporary skill provides basic child execution, Linear integration, and context budget tracking until native Polaris taskchains are implemented in Cluster 6. It does not use `polaris loop`, `polaris map`, or `polaris finalize` — those commands do not exist yet.
