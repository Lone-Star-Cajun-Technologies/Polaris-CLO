{
  "name": "polaris",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "polaris": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.4.0"
  }
}
