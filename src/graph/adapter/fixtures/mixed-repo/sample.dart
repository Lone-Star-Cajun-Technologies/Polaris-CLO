import "dart:async";
import "package:flutter/widgets.dart";

class WorkerCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

WorkerCard buildWorker() {
  return WorkerCard();
}
