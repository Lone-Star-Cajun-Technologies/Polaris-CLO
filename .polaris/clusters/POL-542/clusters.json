{
  "schemaVersion": "v2",
  "source": {
    "id": "POL-542",
    "type": "Linear",
    "analysis": {
      "id": "initial-sync",
      "doc": "Synchronized from Linear issue identifier: POL-542"
    }
  },
  "nodes": {
    "POL-542": {
      "id": "POL-542",
      "title": "IMPLEMENT: QC Repair-Loop Operator Resolution Path",
      "status": "Backlog",
      "body": "## Objective\n\nProvide an operator-facing way to resolve a QC repair loop that terminated with `operator-review` or `medic-referral`, so `polaris finalize` can proceed once a human has reviewed the findings — without hand-editing `state.qc_repair_loop.terminal_outcome` in the state file.\n\n## Context\n\nDiscovered while finalizing [POL-494](https://linear.app/lsctech/issue/POL-494/implement-provider-routing-evidence). All 7 children landed cleanly, but `polaris finalize run` blocked at step 5.8/14:\n\n```\nQC completed-cluster blocked finalize: coderabbit: findings (10 findings)\nQC repair loop terminated with untrusted outcome: \"medic-referral\". Only pass, qc-disabled, no-repairable outcomes allow finalize to proceed. Resolve the repair loop before re-running finalize.\n```\n\nInvestigation found the 10 findings were all false positives against untracked local runtime scratch (`.polaris/tmp/*`, `.polaris/clusters/POL-494/state.json`, `clusters.json`, several `results/*.json` files) — none of these files were ever tracked or committed on the delivery branch. Nothing was at risk of shipping in the PR. But there was no way to formally close the QC gate after confirming this:\n\n1. `polaris medic packet <cluster-id>` — the command `.polaris/skills/polaris-run/chain.md` documents for Medic dispatch on a failed/blocked QC state — **does not exist** in the CLI. This is a doc/implementation mismatch.\n2. `--bypass-medic <reason>` **on** `polaris finalize run` — despite the name, this flag gates an entirely different check (the \"run-health Medic gate\"). It has zero effect on the QC repair-loop gate.\n3. Per `src/qc/POLARIS.md`, `operator-review` findings are meant to settle directly with a human — by design, not automated repair. But no command lets that human decision actually update `terminal_outcome` and unblock finalize. `medic-referral` has the same problem.\n\n## Goal\n\nAn operator who has reviewed `operator-review`/`medic-referral` QC findings and determined they are false positives, already-fixed, or acceptable, should have a governed CLI path to record that decision and unblock finalize — with the decision and rationale captured in an artifact, not a silent hand-edit of state.\n\n## Scope\n\nCluster-wide (see [POL-543](https://linear.app/lsctech/issue/POL-543/add-polaris-qc-resolve-command-and-wire-finalize-gate-to-accept-it) for the concrete child scope):\n\n* src/cli/qc.ts (new)\n* src/cli/index.ts\n* src/qc/repair-loop.ts\n* src/qc/types.ts\n* src/finalize/index.ts\n* src/qc/POLARIS.md\n* .polaris/skills/polaris-run/chain.md\n* .polaris/skills/polaris-run/SKILL.md\n\n## Acceptance Criteria\n\n- [ ] `polaris qc resolve` exists and writes an auditable resolution artifact.\n- [ ] `finalize`'s QC repair-loop gate accepts a valid resolution artifact and proceeds.\n- [ ] Docs no longer reference the non-existent `polaris medic packet <cluster-id>` command.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/qc src/finalize src/cli\n\n## Ordering\n\n* Single child ([POL-543](https://linear.app/lsctech/issue/POL-543/add-polaris-qc-resolve-command-and-wire-finalize-gate-to-accept-it)) — no internal cluster ordering.\n\n## Non-goals\n\n* Do not change QC finding routing/severity policy.\n* Do not weaken the gate to silently trust `medic-referral`/`operator-review` without an explicit recorded decision.\n* Do not retroactively unblock [POL-494](https://linear.app/lsctech/issue/POL-494/implement-provider-routing-evidence)'s finalize as part of this fix.\n\n## Evidence\n\n* Blocked finalize run: cluster [POL-494](https://linear.app/lsctech/issue/POL-494/implement-provider-routing-evidence), run `polaris-run-pol-494-2026-07-11-001`.\n* Repair packet manifest: `.polaris/clusters/POL-494/qc/repair-rounds/1/repair-packets.json`."
    },
    "POL-494": {
      "id": "POL-494",
      "title": "IMPLEMENT: Provider Routing Evidence",
      "status": "Todo"
    },
    "POL-543": {
      "id": "POL-543",
      "title": "Add polaris qc resolve command and wire finalize gate to accept it",
      "status": "Backlog",
      "body": "## Objective\n\nImplement `polaris qc resolve` — a CLI command that lets an operator formally resolve a QC repair loop stuck at `operator-review` or `medic-referral`, and wire `finalize`'s QC gate to accept that resolution.\n\n## Context\n\nParent issue [POL-542](https://linear.app/lsctech/issue/POL-542/implement-qc-repair-loop-operator-resolution-path) documents the gap: `finalize`'s QC repair-loop gate (`checkQcRepairLoopGate` in `src/finalize/index.ts:335-368`) only accepts `terminal_outcome` values `pass`, `qc-disabled`, `no-repairable` (`TRUSTED_QC_REPAIR_OUTCOMES`). `operator-review` and `medic-referral` are terminal states an operator must resolve by design (per `src/qc/POLARIS.md`: \"operator-review packets settle the terminal operator-review outcome directly without worker dispatch\"), but no command exists to record that resolution. The `--bypass-medic` flag on `polaris finalize run` gates an unrelated check (the run-health Medic gate) and has no effect here.\n\nFollow the existing `medic chart create` pattern (`src/cli/medic.ts`) for a CLI command that scaffolds/writes a durable, auditable artifact — this is the closest prior art in the codebase for \"an operator records a decision as a file.\"\n\n## Goal\n\n`polaris qc resolve --cluster-id <id> --outcome <no-repairable|pass> --reason \"<text>\" [--findings <id1,id2,...>]` writes a resolution artifact, and `finalize`'s gate check recognizes it without requiring `state.qc_repair_loop.terminal_outcome` itself to already be a trusted value.\n\n## Scope\n\n* src/cli/qc.ts (new file — mirror `src/cli/medic.ts` structure)\n* src/cli/index.ts (register the new `qc` command)\n* src/qc/repair-loop.ts\n* src/qc/types.ts\n* src/finalize/index.ts (`checkQcRepairLoopGate` and `TRUSTED_QC_REPAIR_OUTCOMES` usage around lines 335-368)\n* src/qc/POLARIS.md\n* .polaris/skills/polaris-run/chain.md\n* .polaris/skills/polaris-run/SKILL.md\n\n## Acceptance Criteria\n\n- [ ] `polaris qc resolve --cluster-id <id> --outcome <no-repairable|pass> --reason \"<text>\"` writes a resolution artifact to `.polaris/clusters/<cluster-id>/qc/repair-rounds/<round>/resolution.json` containing: resolver identity (git user or `$USER`), reason, timestamp, resolved outcome, and the finding IDs from the round's repair-packets.json that this resolution addresses.\n- [ ] `--findings <id1,id2,...>` lets the operator explicitly list which finding IDs are covered; if omitted, defaults to all findings referenced by the round's `repair-packets.json`.\n- [ ] `checkQcRepairLoopGate` in `src/finalize/index.ts` treats a valid resolution artifact for the current round as equivalent to a trusted `terminal_outcome` — finalize proceeds without mutating `state.qc_repair_loop.terminal_outcome` directly.\n- [ ] The resolution artifact requires a non-empty `--reason`; the command errors without one.\n- [ ] `.polaris/skills/polaris-run/chain.md` and `SKILL.md` no longer reference `polaris medic packet <cluster-id>` (which does not exist) for Medic dispatch — replace with the actual mechanism, or remove the reference if none is implemented as part of this change.\n- [ ] `src/qc/POLARIS.md` documents the resolution artifact path and the new gate behavior.\n- [ ] Tests: gate blocks with no resolution artifact and untrusted `terminal_outcome`; gate passes with a valid resolution artifact; `qc resolve` rejects missing `--reason`; `qc resolve` correctly captures finding IDs from `repair-packets.json` when `--findings` is omitted.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/qc src/finalize src/cli\n\n## Ordering\n\n* None — this is the only child of [POL-542](https://linear.app/lsctech/issue/POL-542/implement-qc-repair-loop-operator-resolution-path).\n\n## Non-goals\n\n* Do not change QC finding routing/severity policy (`src/qc/routing.ts`, `src/qc/policy.ts`).\n* Do not weaken the gate to silently trust `medic-referral`/`operator-review` without an explicit recorded resolution — a resolution artifact must always be present.\n* Do not implement a full Medic-dispatch automation for `medic-referral` — that's a larger feature; this issue only provides the operator resolution path.\n* Do not retroactively resolve [POL-494](https://linear.app/lsctech/issue/POL-494/implement-provider-routing-evidence)'s blocked finalize as part of this change."
    }
  },
  "dependencies": {},
  "clusters": {
    "POL-542": {
      "id": "POL-542",
      "title": "Linear Issue: POL-542",
      "children": [
        "POL-543"
      ],
      "cluster_root": "POL-542"
    }
  },
  "activeCluster": "POL-542"
}