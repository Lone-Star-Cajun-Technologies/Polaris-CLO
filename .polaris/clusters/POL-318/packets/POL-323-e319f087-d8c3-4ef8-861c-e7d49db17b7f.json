{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-318-2026-06-04-001",
  "cluster_id": "POL-318",
  "active_child": "POL-323",
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-323 — POL-323\nWorktree: .\nBranch: pol-318-delivery\n\n## Goal\n## Objective\n\nAudit and harden `runFinalize()` so that every tracker-dependent step skips cleanly when no tracker adapter is configured or when `tracker.adapter === \"local\"`.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Finalize already has partial guards — step 6 checks `config.tracker?.adapter` and step 13 checks `linearEnabled`. However, two gaps remain:\n\n1. Step 13 (`stepUpdateLinear`) uses `state.cluster_id` directly as the Linear issue ID. For trackerless runs, `cluster_id` is a local slug or spec path — passing it to a Linear API call would silently fail or produce a wrong update. The guard is present but the intent could be clearer.\n2. Step 6 logs \"Tracker not configured\" but does not distinguish between `adapter === \"local\"` (intentionally local) and `adapter === undefined` (not yet configured). These should produce different log messages.\n3. The finalize worker packet instruction step 7 (addressed in child 3) and the finalize step log header still say \"Updating Linear...\" even when Linear is not configured — this will be fixed here for the runtime log messages.\n\n## Goal\n\n1. In `src/finalize/index.ts` step 6: add a branch for `adapter === \"local\"` that logs `\"[6/14] Local adapter — skipping remote tracker reconciliation.\"`.\n2. In `src/finalize/index.ts` step 13: rename the log message from `\"Updating Linear...\"` to `\"Updating tracker (if configured)...\"` and add a note when skipping why it was skipped.\n3. In `src/finalize/steps/11-update-linear.ts`: add a log line specifying the issue ID being used and confirming it is a Linear ID (guard against using a spec-path as a Linear issue ID).\n4. Add `tracker.adapter === \"local\"` to the `linearEnabled` resolution so it always evaluates to `false` when local.\n\n## Scope\n\n* src/finalize/index.ts\n* src/finalize/steps/11-update-linear.ts\n\n## Acceptance Criteria\n\n* `adapter === \"local\"` produces a distinct log message in step 6.\n* Step 13 log says \"Updating tracker (if configured)...\" instead of \"Updating Linear...\".\n* `stepUpdateLinear` is never called when `adapter === \"local\"`.\n* All existing finalize tests pass.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/finalize\n\n## Ordering\n\n* Blocked by child 1 (PolarisConfig must have `adapter: \"local\"` in the union before this can compile).\n\n## Non-goals\n\n* Do not change the tracker reconciliation logic for `linear` or `mcp-bridge` adapters.\n* Do not change finalize PR creation or commit logic.\n\n## Scope\nTouch only:\n- src/finalize/index.ts\n- src/finalize/steps/11-update-linear.ts\n\n## Acceptance Criteria\n\n## Validation\n- npm run build\n- npm test\n- npx vitest run src/finalize\n\n## Commit\n[POL-323] POL-323\n\n## Governance\n- Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-323\" from open_children to completed_children.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-323\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-323\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-323\".",
      "Implement POL-323: \"POL-323\".",
      "Issue description:\n## Objective\n\nAudit and harden `runFinalize()` so that every tracker-dependent step skips cleanly when no tracker adapter is configured or when `tracker.adapter === \"local\"`.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Finalize already has partial guards — step 6 checks `config.tracker?.adapter` and step 13 checks `linearEnabled`. However, two gaps remain:\n\n1. Step 13 (`stepUpdateLinear`) uses `state.cluster_id` directly as the Linear issue ID. For trackerless runs, `cluster_id` is a local slug or spec path — passing it to a Linear API call would silently fail or produce a wrong update. The guard is present but the intent could be clearer.\n2. Step 6 logs \"Tracker not configured\" but does not distinguish between `adapter === \"local\"` (intentionally local) and `adapter === undefined` (not yet configured). These should produce different log messages.\n3. The finalize worker packet instruction step 7 (addressed in child 3) and the finalize step log header still say \"Updating Linear...\" even when Linear is not configured — this will be fixed here for the runtime log messages.\n\n## Goal\n\n1. In `src/finalize/index.ts` step 6: add a branch for `adapter === \"local\"` that logs `\"[6/14] Local adapter — skipping remote tracker reconciliation.\"`.\n2. In `src/finalize/index.ts` step 13: rename the log message from `\"Updating Linear...\"` to `\"Updating tracker (if configured)...\"` and add a note when skipping why it was skipped.\n3. In `src/finalize/steps/11-update-linear.ts`: add a log line specifying the issue ID being used and confirming it is a Linear ID (guard against using a spec-path as a Linear issue ID).\n4. Add `tracker.adapter === \"local\"` to the `linearEnabled` resolution so it always evaluates to `false` when local.\n\n## Scope\n\n* src/finalize/index.ts\n* src/finalize/steps/11-update-linear.ts\n\n## Acceptance Criteria\n\n* `adapter === \"local\"` produces a distinct log message in step 6.\n* Step 13 log says \"Updating tracker (if configured)...\" instead of \"Updating Linear...\".\n* `stepUpdateLinear` is never called when `adapter === \"local\"`.\n* All existing finalize tests pass.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/finalize\n\n## Ordering\n\n* Blocked by child 1 (PolarisConfig must have `adapter: \"local\"` in the union before this can compile).\n\n## Non-goals\n\n* Do not change the tracker reconciliation logic for `linear` or `mcp-bridge` adapters.\n* Do not change finalize PR creation or commit logic.",
      "Requirements:",
      "   1. `adapter === \"local\"` produces a distinct log message in step 6.",
      "   2. Step 13 log says \"Updating tracker (if configured)...\" instead of \"Updating Linear...\".",
      "   3. `stepUpdateLinear` is never called when `adapter === \"local\"`.",
      "   4. All existing finalize tests pass.",
      "   5. `npm run build` passes.",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-323] POL-323.",
      "Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-323\" from open_children to completed_children.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action).",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "src/finalize/index.ts",
      "src/finalize/steps/11-update-linear.ts"
    ],
    "validation_commands": [
      "npm run build",
      "npm test",
      "npx vitest run src/finalize"
    ],
    "issue_context": {
      "id": "POL-323",
      "title": "POL-323",
      "key_requirements": [],
      "body": "## Objective\n\nAudit and harden `runFinalize()` so that every tracker-dependent step skips cleanly when no tracker adapter is configured or when `tracker.adapter === \"local\"`.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Finalize already has partial guards — step 6 checks `config.tracker?.adapter` and step 13 checks `linearEnabled`. However, two gaps remain:\n\n1. Step 13 (`stepUpdateLinear`) uses `state.cluster_id` directly as the Linear issue ID. For trackerless runs, `cluster_id` is a local slug or spec path — passing it to a Linear API call would silently fail or produce a wrong update. The guard is present but the intent could be clearer.\n2. Step 6 logs \"Tracker not configured\" but does not distinguish between `adapter === \"local\"` (intentionally local) and `adapter === undefined` (not yet configured). These should produce different log messages.\n3. The finalize worker packet instruction step 7 (addressed in child 3) and the finalize step log header still say \"Updating Linear...\" even when Linear is not configured — this will be fixed here for the runtime log messages.\n\n## Goal\n\n1. In `src/finalize/index.ts` step 6: add a branch for `adapter === \"local\"` that logs `\"[6/14] Local adapter — skipping remote tracker reconciliation.\"`.\n2. In `src/finalize/index.ts` step 13: rename the log message from `\"Updating Linear...\"` to `\"Updating tracker (if configured)...\"` and add a note when skipping why it was skipped.\n3. In `src/finalize/steps/11-update-linear.ts`: add a log line specifying the issue ID being used and confirming it is a Linear ID (guard against using a spec-path as a Linear issue ID).\n4. Add `tracker.adapter === \"local\"` to the `linearEnabled` resolution so it always evaluates to `false` when local.\n\n## Scope\n\n* src/finalize/index.ts\n* src/finalize/steps/11-update-linear.ts\n\n## Acceptance Criteria\n\n* `adapter === \"local\"` produces a distinct log message in step 6.\n* Step 13 log says \"Updating tracker (if configured)...\" instead of \"Updating Linear...\".\n* `stepUpdateLinear` is never called when `adapter === \"local\"`.\n* All existing finalize tests pass.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/finalize\n\n## Ordering\n\n* Blocked by child 1 (PolarisConfig must have `adapter: \"local\"` in the union before this can compile).\n\n## Non-goals\n\n* Do not change the tracker reconciliation logic for `linear` or `mcp-bridge` adapters.\n* Do not change finalize PR creation or commit logic."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 7034,
    "estimated_tokens": 1759
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "prohibited_write_paths": [
    ".taskchain_artifacts/",
    ".polaris/clusters/",
    ".polaris/runs/",
    "**/telemetry.jsonl"
  ],
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-318/results/POL-323-e319f087-d8c3-4ef8-861c-e7d49db17b7f.json",
    "result_required_fields": {
      "run_id": "polaris-run-pol-318-2026-06-04-001",
      "cluster_id": "POL-318",
      "child_id": "<child_id>",
      "status": "<status>",
      "commit": "<commit>",
      "validation": "<validation>",
      "next_recommended_action": "<next_recommended_action>"
    }
  },
  "context": {
    "branch": "pol-318-delivery",
    "worker_role": "impl"
  }
}