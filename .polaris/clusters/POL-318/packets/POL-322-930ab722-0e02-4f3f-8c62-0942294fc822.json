{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-318-2026-06-04-001",
  "cluster_id": "POL-318",
  "active_child": "POL-322",
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-322 — POL-322\nWorktree: .\nBranch: pol-318-delivery\n\n## Goal\n## Objective\n\nCreate a SpecAdapter that reads a markdown spec file and produces a LocalGraph, and wire `polaris analyze spec <path>` and `polaris run spec <path>` CLI entry points to use it.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Currently there is no way to invoke `polaris run` or `polaris analyze` on a local spec file — both commands require a Linear issue ID. A `SpecAdapter` reads a structured markdown file (with `## Objective`, `## Scope`, `## Validation`, `## Children` sections) and produces an `ExecutionGraphV2` with `source.type: \"spec\"`, allowing the rest of the runtime (bootstrap, dispatch, finalize) to operate without a tracker.\n\n## Goal\n\n1. Create `src/tracker/adapters/spec/index.ts` with a `SpecAdapter` class that implements `syncIn(specPath)` and returns a `LocalGraph`. The adapter reads the canonical section format from the spec file and maps sections to `ExecutionNode` fields.\n2. Create `src/tracker/adapters/local/index.ts` with a `LocalAdapter` that wraps an already-present `clusters.json` on disk (for the `tracker.adapter: \"local\"` config case).\n3. Wire `polaris analyze spec <path>` in the CLI (extend the `analyze` or add a `spec` subcommand).\n4. Wire `polaris run spec <path>` — reads the spec, calls `SpecAdapter.syncIn()`, saves the resulting `clusters.json`, then runs normal bootstrap.\n5. Local child IDs use slugs derived from spec section headers, e.g. `spec-child-01`, `spec-child-02`.\n\n## Scope\n\n* src/tracker/adapters/spec/index.ts\n* src/tracker/adapters/local/index.ts\n* src/cli/index.ts\n* src/cli/args.ts\n\n## Acceptance Criteria\n\n* `SpecAdapter.syncIn(path)` returns a valid `LocalGraph` from a spec markdown file.\n* `polaris run spec <path>` bootstraps and dispatches children without requiring a tracker.\n* Local child IDs are deterministic slugs (not UUIDs).\n* `source.type` in the resulting graph is `\"spec\"`.\n* Unit test for `SpecAdapter.syncIn()` with a fixture spec file passes.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/tracker/adapters/spec\n\n## Ordering\n\n* Blocked by child 2 (source-agnostic graph loading must be in place first).\n\n## Non-goals\n\n* Do not implement GitHub issue intake in this child (prompt and GitHub sources are out of scope for this cluster).\n* Do not modify finalize.\n\n## Scope\nTouch only:\n- src/tracker/adapters/spec/index.ts\n- src/tracker/adapters/local/index.ts\n- src/cli/index.ts\n- src/cli/args.ts\n\n## Acceptance Criteria\n\n## Validation\n- npm run build\n- npm test\n- npx vitest run src/tracker/adapters/spec\n\n## Commit\n[POL-322] POL-322\n\n## Governance\n- Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-322\" from open_children to completed_children.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-322\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-322\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-322\".",
      "Implement POL-322: \"POL-322\".",
      "Issue description:\n## Objective\n\nCreate a SpecAdapter that reads a markdown spec file and produces a LocalGraph, and wire `polaris analyze spec <path>` and `polaris run spec <path>` CLI entry points to use it.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Currently there is no way to invoke `polaris run` or `polaris analyze` on a local spec file — both commands require a Linear issue ID. A `SpecAdapter` reads a structured markdown file (with `## Objective`, `## Scope`, `## Validation`, `## Children` sections) and produces an `ExecutionGraphV2` with `source.type: \"spec\"`, allowing the rest of the runtime (bootstrap, dispatch, finalize) to operate without a tracker.\n\n## Goal\n\n1. Create `src/tracker/adapters/spec/index.ts` with a `SpecAdapter` class that implements `syncIn(specPath)` and returns a `LocalGraph`. The adapter reads the canonical section format from the spec file and maps sections to `ExecutionNode` fields.\n2. Create `src/tracker/adapters/local/index.ts` with a `LocalAdapter` that wraps an already-present `clusters.json` on disk (for the `tracker.adapter: \"local\"` config case).\n3. Wire `polaris analyze spec <path>` in the CLI (extend the `analyze` or add a `spec` subcommand).\n4. Wire `polaris run spec <path>` — reads the spec, calls `SpecAdapter.syncIn()`, saves the resulting `clusters.json`, then runs normal bootstrap.\n5. Local child IDs use slugs derived from spec section headers, e.g. `spec-child-01`, `spec-child-02`.\n\n## Scope\n\n* src/tracker/adapters/spec/index.ts\n* src/tracker/adapters/local/index.ts\n* src/cli/index.ts\n* src/cli/args.ts\n\n## Acceptance Criteria\n\n* `SpecAdapter.syncIn(path)` returns a valid `LocalGraph` from a spec markdown file.\n* `polaris run spec <path>` bootstraps and dispatches children without requiring a tracker.\n* Local child IDs are deterministic slugs (not UUIDs).\n* `source.type` in the resulting graph is `\"spec\"`.\n* Unit test for `SpecAdapter.syncIn()` with a fixture spec file passes.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/tracker/adapters/spec\n\n## Ordering\n\n* Blocked by child 2 (source-agnostic graph loading must be in place first).\n\n## Non-goals\n\n* Do not implement GitHub issue intake in this child (prompt and GitHub sources are out of scope for this cluster).\n* Do not modify finalize.",
      "Requirements:",
      "   1. `SpecAdapter.syncIn(path)` returns a valid `LocalGraph` from a spec markdown file.",
      "   2. `polaris run spec <path>` bootstraps and dispatches children without requiring a tracker.",
      "   3. Local child IDs are deterministic slugs (not UUIDs).",
      "   4. `source.type` in the resulting graph is `\"spec\"`.",
      "   5. Unit test for `SpecAdapter.syncIn()` with a fixture spec file passes.",
      "   6. `npm run build` passes.",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-322] POL-322.",
      "Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-322\" from open_children to completed_children.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action).",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "src/tracker/adapters/spec/index.ts",
      "src/tracker/adapters/local/index.ts",
      "src/cli/index.ts",
      "src/cli/args.ts"
    ],
    "validation_commands": [
      "npm run build",
      "npm test",
      "npx vitest run src/tracker/adapters/spec"
    ],
    "issue_context": {
      "id": "POL-322",
      "title": "POL-322",
      "key_requirements": [],
      "body": "## Objective\n\nCreate a SpecAdapter that reads a markdown spec file and produces a LocalGraph, and wire `polaris analyze spec <path>` and `polaris run spec <path>` CLI entry points to use it.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Currently there is no way to invoke `polaris run` or `polaris analyze` on a local spec file — both commands require a Linear issue ID. A `SpecAdapter` reads a structured markdown file (with `## Objective`, `## Scope`, `## Validation`, `## Children` sections) and produces an `ExecutionGraphV2` with `source.type: \"spec\"`, allowing the rest of the runtime (bootstrap, dispatch, finalize) to operate without a tracker.\n\n## Goal\n\n1. Create `src/tracker/adapters/spec/index.ts` with a `SpecAdapter` class that implements `syncIn(specPath)` and returns a `LocalGraph`. The adapter reads the canonical section format from the spec file and maps sections to `ExecutionNode` fields.\n2. Create `src/tracker/adapters/local/index.ts` with a `LocalAdapter` that wraps an already-present `clusters.json` on disk (for the `tracker.adapter: \"local\"` config case).\n3. Wire `polaris analyze spec <path>` in the CLI (extend the `analyze` or add a `spec` subcommand).\n4. Wire `polaris run spec <path>` — reads the spec, calls `SpecAdapter.syncIn()`, saves the resulting `clusters.json`, then runs normal bootstrap.\n5. Local child IDs use slugs derived from spec section headers, e.g. `spec-child-01`, `spec-child-02`.\n\n## Scope\n\n* src/tracker/adapters/spec/index.ts\n* src/tracker/adapters/local/index.ts\n* src/cli/index.ts\n* src/cli/args.ts\n\n## Acceptance Criteria\n\n* `SpecAdapter.syncIn(path)` returns a valid `LocalGraph` from a spec markdown file.\n* `polaris run spec <path>` bootstraps and dispatches children without requiring a tracker.\n* Local child IDs are deterministic slugs (not UUIDs).\n* `source.type` in the resulting graph is `\"spec\"`.\n* Unit test for `SpecAdapter.syncIn()` with a fixture spec file passes.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/tracker/adapters/spec\n\n## Ordering\n\n* Blocked by child 2 (source-agnostic graph loading must be in place first).\n\n## Non-goals\n\n* Do not implement GitHub issue intake in this child (prompt and GitHub sources are out of scope for this cluster).\n* Do not modify finalize."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 6921,
    "estimated_tokens": 1730
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "prohibited_write_paths": [
    ".taskchain_artifacts/",
    ".polaris/clusters/",
    ".polaris/runs/",
    "**/telemetry.jsonl"
  ],
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-318/results/POL-322-930ab722-0e02-4f3f-8c62-0942294fc822.json",
    "result_required_fields": {
      "run_id": "polaris-run-pol-318-2026-06-04-001",
      "cluster_id": "POL-318",
      "child_id": "<child_id>",
      "status": "<status>",
      "commit": "<commit>",
      "validation": "<validation>",
      "next_recommended_action": "<next_recommended_action>"
    }
  },
  "context": {
    "branch": "pol-318-delivery",
    "worker_role": "impl"
  }
}