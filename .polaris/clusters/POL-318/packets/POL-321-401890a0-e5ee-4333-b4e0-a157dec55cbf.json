{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-318-2026-06-04-001",
  "cluster_id": "POL-318",
  "active_child": "POL-321",
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-321 — POL-321\nWorktree: .\nBranch: pol-318-delivery\n\n## Goal\n## Objective\n\nReplace all hardcoded \"Linear\" references in the packet compiler instruction text and skill packet generator output with tracker-conditional or tracker-agnostic language.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Two files contain hardcoded Linear text that will be wrong for trackerless runs:\n\n1. `src/loop/worker-packet.ts:compileFinalizePacket()` — step 7 reads `\"Update Linear parent issue ${input.clusterId} to Done.\"` — but finalize step 13 already has a `linearEnabled` guard. The instruction text should match the actual runtime behavior.\n2. `src/skill-packet/generator.ts:buildAnalyzePacket()` — `allowed_outputs` contains `\"Linear issue descriptions and child issues\"` and `deliverables` contains `\"Implementation-ready Linear issues, or\"` — these are wrong for trackerless or GitHub-issue-backed analyze sessions.\n\n## Goal\n\n1. In `compileFinalizePacket()`, change step 7 to: `\"If tracker is configured: update the parent issue ${input.clusterId} to In Review / Done per your tracker adapter. If no tracker: skip this step.\"` — or make it conditional on a `trackerEnabled` input flag passed to the compiler.\n2. In `buildAnalyzePacket()`, change `allowed_outputs` and `deliverables` to use tracker-agnostic language: `\"Tracker child issues or local work unit files (depending on work source)\"` and `\"Implementation-ready work units (tracker issues or local work unit files)\"`.\n\n## Scope\n\n* src/loop/worker-packet.ts\n* src/skill-packet/generator.ts\n\n## Acceptance Criteria\n\n* `compileFinalizePacket()` step 7 contains no hardcoded `\"Linear\"` string.\n* `buildAnalyzePacket()` `allowed_outputs` and `deliverables` contain no hardcoded `\"Linear\"` string.\n* All existing tests for these files pass.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/loop/worker-packet\n* npx vitest run src/skill-packet/generator\n\n## Ordering\n\n* Blocked by the first child (WorkContract types must exist before this runs).\n\n## Non-goals\n\n* Do not change the packet structure or return contracts.\n* Do not modify finalize runtime behavior (that is handled in a separate child).\n\n## Scope\nTouch only:\n- src/loop/worker-packet.ts\n- src/skill-packet/generator.ts\n\n## Acceptance Criteria\n\n## Validation\n- npm run build\n- npm test\n- npx vitest run src/loop/worker-packet\n- npx vitest run src/skill-packet/generator\n\n## Commit\n[POL-321] POL-321\n\n## Governance\n- Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-321\" from open_children to completed_children.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-321\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-321\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-321\".",
      "Implement POL-321: \"POL-321\".",
      "Issue description:\n## Objective\n\nReplace all hardcoded \"Linear\" references in the packet compiler instruction text and skill packet generator output with tracker-conditional or tracker-agnostic language.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Two files contain hardcoded Linear text that will be wrong for trackerless runs:\n\n1. `src/loop/worker-packet.ts:compileFinalizePacket()` — step 7 reads `\"Update Linear parent issue ${input.clusterId} to Done.\"` — but finalize step 13 already has a `linearEnabled` guard. The instruction text should match the actual runtime behavior.\n2. `src/skill-packet/generator.ts:buildAnalyzePacket()` — `allowed_outputs` contains `\"Linear issue descriptions and child issues\"` and `deliverables` contains `\"Implementation-ready Linear issues, or\"` — these are wrong for trackerless or GitHub-issue-backed analyze sessions.\n\n## Goal\n\n1. In `compileFinalizePacket()`, change step 7 to: `\"If tracker is configured: update the parent issue ${input.clusterId} to In Review / Done per your tracker adapter. If no tracker: skip this step.\"` — or make it conditional on a `trackerEnabled` input flag passed to the compiler.\n2. In `buildAnalyzePacket()`, change `allowed_outputs` and `deliverables` to use tracker-agnostic language: `\"Tracker child issues or local work unit files (depending on work source)\"` and `\"Implementation-ready work units (tracker issues or local work unit files)\"`.\n\n## Scope\n\n* src/loop/worker-packet.ts\n* src/skill-packet/generator.ts\n\n## Acceptance Criteria\n\n* `compileFinalizePacket()` step 7 contains no hardcoded `\"Linear\"` string.\n* `buildAnalyzePacket()` `allowed_outputs` and `deliverables` contain no hardcoded `\"Linear\"` string.\n* All existing tests for these files pass.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/loop/worker-packet\n* npx vitest run src/skill-packet/generator\n\n## Ordering\n\n* Blocked by the first child (WorkContract types must exist before this runs).\n\n## Non-goals\n\n* Do not change the packet structure or return contracts.\n* Do not modify finalize runtime behavior (that is handled in a separate child).",
      "Requirements:",
      "   1. `compileFinalizePacket()` step 7 contains no hardcoded `\"Linear\"` string.",
      "   2. `buildAnalyzePacket()` `allowed_outputs` and `deliverables` contain no hardcoded `\"Linear\"` string.",
      "   3. All existing tests for these files pass.",
      "   4. `npm run build` passes.",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-321] POL-321.",
      "Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-321\" from open_children to completed_children.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-318-2026-06-04-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action).",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "src/loop/worker-packet.ts",
      "src/skill-packet/generator.ts"
    ],
    "validation_commands": [
      "npm run build",
      "npm test",
      "npx vitest run src/loop/worker-packet",
      "npx vitest run src/skill-packet/generator"
    ],
    "issue_context": {
      "id": "POL-321",
      "title": "POL-321",
      "key_requirements": [],
      "body": "## Objective\n\nReplace all hardcoded \"Linear\" references in the packet compiler instruction text and skill packet generator output with tracker-conditional or tracker-agnostic language.\n\n## Context\n\nPart of [POL-318](https://linear.app/lsctech/issue/POL-318/implement-decouple-packets-from-tracker-assumptions-with-spec-to-work). Two files contain hardcoded Linear text that will be wrong for trackerless runs:\n\n1. `src/loop/worker-packet.ts:compileFinalizePacket()` — step 7 reads `\"Update Linear parent issue ${input.clusterId} to Done.\"` — but finalize step 13 already has a `linearEnabled` guard. The instruction text should match the actual runtime behavior.\n2. `src/skill-packet/generator.ts:buildAnalyzePacket()` — `allowed_outputs` contains `\"Linear issue descriptions and child issues\"` and `deliverables` contains `\"Implementation-ready Linear issues, or\"` — these are wrong for trackerless or GitHub-issue-backed analyze sessions.\n\n## Goal\n\n1. In `compileFinalizePacket()`, change step 7 to: `\"If tracker is configured: update the parent issue ${input.clusterId} to In Review / Done per your tracker adapter. If no tracker: skip this step.\"` — or make it conditional on a `trackerEnabled` input flag passed to the compiler.\n2. In `buildAnalyzePacket()`, change `allowed_outputs` and `deliverables` to use tracker-agnostic language: `\"Tracker child issues or local work unit files (depending on work source)\"` and `\"Implementation-ready work units (tracker issues or local work unit files)\"`.\n\n## Scope\n\n* src/loop/worker-packet.ts\n* src/skill-packet/generator.ts\n\n## Acceptance Criteria\n\n* `compileFinalizePacket()` step 7 contains no hardcoded `\"Linear\"` string.\n* `buildAnalyzePacket()` `allowed_outputs` and `deliverables` contain no hardcoded `\"Linear\"` string.\n* All existing tests for these files pass.\n* `npm run build` passes.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/loop/worker-packet\n* npx vitest run src/skill-packet/generator\n\n## Ordering\n\n* Blocked by the first child (WorkContract types must exist before this runs).\n\n## Non-goals\n\n* Do not change the packet structure or return contracts.\n* Do not modify finalize runtime behavior (that is handled in a separate child)."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 6731,
    "estimated_tokens": 1683
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "prohibited_write_paths": [
    ".taskchain_artifacts/",
    ".polaris/clusters/",
    ".polaris/runs/",
    "**/telemetry.jsonl"
  ],
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-318/results/POL-321-401890a0-e5ee-4333-b4e0-a157dec55cbf.json",
    "result_required_fields": {
      "run_id": "polaris-run-pol-318-2026-06-04-001",
      "cluster_id": "POL-318",
      "child_id": "<child_id>",
      "status": "<status>",
      "commit": "<commit>",
      "validation": "<validation>",
      "next_recommended_action": "<next_recommended_action>"
    }
  },
  "context": {
    "branch": "pol-318-delivery",
    "worker_role": "impl"
  }
}