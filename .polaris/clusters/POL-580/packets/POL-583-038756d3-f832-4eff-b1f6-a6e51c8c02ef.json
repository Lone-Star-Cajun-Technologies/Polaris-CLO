{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-paperclip-role-routing-2026-07-28-002",
  "cluster_id": "POL-580",
  "active_child": "POL-583",
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-paperclip-role-routing-2026-07-28-002/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-583 — POL-583\nWorktree: .\nBranch: philmeaux/pol-580-implement-paperclip-role-based-assignee-routing\n\n## Goal\n## Objective\n\nStop the adapter from trusting Paperclip's own `status: \"done\"` as sufficient evidence of completion.\n\n## Context\n\n`waitForPaperclipExecution()` (`src/loop/adapters/paperclip.ts:542-570`) polls `getPaperclipIssue()` and returns as soon as Paperclip's `status` is `done`/`cancelled`/`failed`/`blocked`. `dispatch()` (`paperclip.ts:587-696`) then returns `exit_code: 0` purely on `status: \"done\"` — no check for a linked PR, commit, branch, or other work product. Paperclip's own agent-facing protocol (fetched from the VPS host's `AGENTS.md`) explicitly warns that \"comments, documents, screenshots, work products, and `Remaining` bullets are evidence, not valid liveness paths by themselves\" and expects a `pull_request`/`commit`/`branch` work product for operator-facing deliverables — this child makes the Polaris side actually check for that.\n\n## Goal\n\nBefore `dispatch()` returns `exit_code: 0` for a `status: \"done\"` issue, verify via `getPaperclipIssue()` (or a new typed field on its response, if the current response type doesn't expose work products/attachments) that at least one PR/commit/branch/work-product reference exists. If none is found, return a failure result (`exit_code: 2`, `failure_category` describing missing evidence) instead of success.\n\n## Scope\n\n* src/loop/adapters/paperclip.ts\n* src/loop/adapters/paperclip.test.ts\n* src/loop/adapters/**tests**/paperclip.test.ts\n\n## Acceptance Criteria\n\n* `status: \"done\"` with a linked PR/commit/branch/work-product still returns `exit_code: 0`.\n* `status: \"done\"` with no such evidence returns a non-zero exit code and a descriptive failure message — the run does not silently advance.\n* `status: \"blocked\"`/`\"cancelled\"`/`\"failed\"` behavior is unchanged (already surfaced as non-success today, per existing tests).\n* If `getPaperclipIssue()`'s current response type doesn't expose enough structured data to detect evidence, extend its typing/parsing rather than parsing free-text comments; note this as a risk if the Paperclip API turns out not to expose it at all, and report back instead of guessing at an undocumented field.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/loop/adapters\n\n## Ordering\n\n* None — independent of [POL-581](https://linear.app/lsctech/issue/POL-581/add-rolebindings-reportsto-fallback-to-paperclipexecutionconfig-schema)/[POL-582](https://linear.app/lsctech/issue/POL-582/wire-role-aware-assignee-resolution-into-paperclip-adapter-dispatch) (different code path within the same file; safe to land in parallel or before/after).\n\n## Non-goals\n\n* Do not change `src/finalize/delivery-integrity.ts` — that check remains as a separate, later safety net.\n\n## Implementation Discipline\nBefore writing code, stop at the first rung that holds:\n\n1. Does this need to exist?   → no: skip it (YAGNI)\n2. Stdlib does it?            → use it\n3. Native platform feature?   → use it\n4. Installed dependency?      → use it\n5. One line?                  → one line\n6. Only then: the minimum that works\n\nLazy, not negligent: validation, error handling, security, and accessibility are never on the chopping block.\n\nWhen you defer a simplification for later, mark it inline:\n  // ponytail: <what you deferred and why>\n\n## Scope\nTouch only:\n- src/loop/adapters/paperclip.ts\n- src/loop/adapters/paperclip.test.ts\n- src/loop/adapters/**tests**/paperclip.test.ts\n\n## Acceptance Criteria\n\n## Validation\n- npm run build\n- npm test\n- npx vitest run src/loop/adapters\n\n## Commit\n[POL-583] POL-583\n\n## Governance\n- Do NOT modify open_children or completed_children in /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json. The parent runtime (loop continue) owns state transitions.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-paperclip-role-routing-2026-07-28-002/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-paperclip-role-routing-2026-07-28-002/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-583\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-583\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-583\".",
      "Implement POL-583: \"POL-583\".",
      "Issue description:\n## Objective\n\nStop the adapter from trusting Paperclip's own `status: \"done\"` as sufficient evidence of completion.\n\n## Context\n\n`waitForPaperclipExecution()` (`src/loop/adapters/paperclip.ts:542-570`) polls `getPaperclipIssue()` and returns as soon as Paperclip's `status` is `done`/`cancelled`/`failed`/`blocked`. `dispatch()` (`paperclip.ts:587-696`) then returns `exit_code: 0` purely on `status: \"done\"` — no check for a linked PR, commit, branch, or other work product. Paperclip's own agent-facing protocol (fetched from the VPS host's `AGENTS.md`) explicitly warns that \"comments, documents, screenshots, work products, and `Remaining` bullets are evidence, not valid liveness paths by themselves\" and expects a `pull_request`/`commit`/`branch` work product for operator-facing deliverables — this child makes the Polaris side actually check for that.\n\n## Goal\n\nBefore `dispatch()` returns `exit_code: 0` for a `status: \"done\"` issue, verify via `getPaperclipIssue()` (or a new typed field on its response, if the current response type doesn't expose work products/attachments) that at least one PR/commit/branch/work-product reference exists. If none is found, return a failure result (`exit_code: 2`, `failure_category` describing missing evidence) instead of success.\n\n## Scope\n\n* src/loop/adapters/paperclip.ts\n* src/loop/adapters/paperclip.test.ts\n* src/loop/adapters/**tests**/paperclip.test.ts\n\n## Acceptance Criteria\n\n* `status: \"done\"` with a linked PR/commit/branch/work-product still returns `exit_code: 0`.\n* `status: \"done\"` with no such evidence returns a non-zero exit code and a descriptive failure message — the run does not silently advance.\n* `status: \"blocked\"`/`\"cancelled\"`/`\"failed\"` behavior is unchanged (already surfaced as non-success today, per existing tests).\n* If `getPaperclipIssue()`'s current response type doesn't expose enough structured data to detect evidence, extend its typing/parsing rather than parsing free-text comments; note this as a risk if the Paperclip API turns out not to expose it at all, and report back instead of guessing at an undocumented field.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/loop/adapters\n\n## Ordering\n\n* None — independent of [POL-581](https://linear.app/lsctech/issue/POL-581/add-rolebindings-reportsto-fallback-to-paperclipexecutionconfig-schema)/[POL-582](https://linear.app/lsctech/issue/POL-582/wire-role-aware-assignee-resolution-into-paperclip-adapter-dispatch) (different code path within the same file; safe to land in parallel or before/after).\n\n## Non-goals\n\n* Do not change `src/finalize/delivery-integrity.ts` — that check remains as a separate, later safety net.",
      "Requirements:",
      "   1. `status: \"done\"` with a linked PR/commit/branch/work-product still returns `exit_code: 0`.",
      "   2. `status: \"done\"` with no such evidence returns a non-zero exit code and a descriptive failure message — the run does not silently advance.",
      "   3. `status: \"blocked\"`/`\"cancelled\"`/`\"failed\"` behavior is unchanged (already surfaced as non-success today, per existing tests).",
      "   4. If `getPaperclipIssue()`'s current response type doesn't expose enough structured data to detect evidence, extend its typing/parsing rather than parsing free-text comments; note this as a risk if the Paperclip API turns out not to expose it at all, and report back instead of guessing at an undocumented field.",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-583] POL-583.",
      "Do NOT modify open_children or completed_children in /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json. The parent runtime (loop continue) owns that state transition.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-paperclip-role-routing-2026-07-28-002/telemetry.jsonl.",
      "If you encountered any struggle signals during execution, include a \"run_health_symptoms\" array in your sealed result. Use one of these categories: worker-blocked, validation-failed, repeated-rework, unclear-requirements, unusual-assumption. Report what you observed — do NOT diagnose root cause. Omit the field if no symptoms occurred.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action). next_recommended_action MUST be exactly \"continue\" on success, \"stop\" on unresolvable blocker, or \"investigate\" if manual review is needed.",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "src/loop/adapters/paperclip.ts",
      "src/loop/adapters/paperclip.test.ts",
      "src/loop/adapters/**tests**/paperclip.test.ts"
    ],
    "validation_commands": [
      "npm run build",
      "npm test",
      "npx vitest run src/loop/adapters"
    ],
    "validation_expectations": [],
    "issue_context": {
      "id": "POL-583",
      "title": "POL-583",
      "key_requirements": [],
      "body": "## Objective\n\nStop the adapter from trusting Paperclip's own `status: \"done\"` as sufficient evidence of completion.\n\n## Context\n\n`waitForPaperclipExecution()` (`src/loop/adapters/paperclip.ts:542-570`) polls `getPaperclipIssue()` and returns as soon as Paperclip's `status` is `done`/`cancelled`/`failed`/`blocked`. `dispatch()` (`paperclip.ts:587-696`) then returns `exit_code: 0` purely on `status: \"done\"` — no check for a linked PR, commit, branch, or other work product. Paperclip's own agent-facing protocol (fetched from the VPS host's `AGENTS.md`) explicitly warns that \"comments, documents, screenshots, work products, and `Remaining` bullets are evidence, not valid liveness paths by themselves\" and expects a `pull_request`/`commit`/`branch` work product for operator-facing deliverables — this child makes the Polaris side actually check for that.\n\n## Goal\n\nBefore `dispatch()` returns `exit_code: 0` for a `status: \"done\"` issue, verify via `getPaperclipIssue()` (or a new typed field on its response, if the current response type doesn't expose work products/attachments) that at least one PR/commit/branch/work-product reference exists. If none is found, return a failure result (`exit_code: 2`, `failure_category` describing missing evidence) instead of success.\n\n## Scope\n\n* src/loop/adapters/paperclip.ts\n* src/loop/adapters/paperclip.test.ts\n* src/loop/adapters/**tests**/paperclip.test.ts\n\n## Acceptance Criteria\n\n* `status: \"done\"` with a linked PR/commit/branch/work-product still returns `exit_code: 0`.\n* `status: \"done\"` with no such evidence returns a non-zero exit code and a descriptive failure message — the run does not silently advance.\n* `status: \"blocked\"`/`\"cancelled\"`/`\"failed\"` behavior is unchanged (already surfaced as non-success today, per existing tests).\n* If `getPaperclipIssue()`'s current response type doesn't expose enough structured data to detect evidence, extend its typing/parsing rather than parsing free-text comments; note this as a risk if the Paperclip API turns out not to expose it at all, and report back instead of guessing at an undocumented field.\n\n## Validation\n\n* npm run build\n* npm test\n* npx vitest run src/loop/adapters\n\n## Ordering\n\n* None — independent of [POL-581](https://linear.app/lsctech/issue/POL-581/add-rolebindings-reportsto-fallback-to-paperclipexecutionconfig-schema)/[POL-582](https://linear.app/lsctech/issue/POL-582/wire-role-aware-assignee-resolution-into-paperclip-adapter-dispatch) (different code path within the same file; safe to land in parallel or before/after).\n\n## Non-goals\n\n* Do not change `src/finalize/delivery-integrity.ts` — that check remains as a separate, later safety net."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 7875,
    "estimated_tokens": 1969
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "routing_context": {
    "task_type": "impl",
    "required_capabilities": [
      "implementation"
    ]
  },
  "prohibited_write_paths": [
    ".taskchain_artifacts/",
    ".polaris/clusters/",
    ".polaris/runs/",
    "**/telemetry.jsonl"
  ],
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-580/results/POL-583-038756d3-f832-4eff-b1f6-a6e51c8c02ef.json",
    "result_required_fields": {
      "run_id": "polaris-run-paperclip-role-routing-2026-07-28-002",
      "cluster_id": "POL-580",
      "child_id": "<child_id>",
      "status": "<status>",
      "commit": "<commit>",
      "validation": "<validation>",
      "next_recommended_action": "<next_recommended_action>"
    }
  },
  "context": {
    "branch": "philmeaux/pol-580-implement-paperclip-role-based-assignee-routing",
    "worker_role": "impl"
  }
}