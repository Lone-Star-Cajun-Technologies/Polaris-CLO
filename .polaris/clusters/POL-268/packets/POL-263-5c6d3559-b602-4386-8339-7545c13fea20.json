{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-268-2026-06-02-001",
  "cluster_id": "POL-268",
  "active_child": "POL-263",
  "validation_waiver": true,
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-263 — POL-263\nWorktree: .\nBranch: main\n\n## Goal\n## Objective\n\nImplement the SmartDocs migration step in `polaris init --adopt` so discovered SmartDocs candidates are moved into `smartdocs/raw/` with provenance recorded.\n\n## Context\n\nThis is Phase C, Step C2 of the adoption workflow. The migration must preserve history when possible, avoid moving excluded files, and remain resumable so repeated runs do not re-move already completed steps.\n\n## Goal\n\nCreate `migrateSmartDocs(plan: AdoptionPlan): Promise<void>` and execute pending SmartDocs migration steps safely, with provenance tracking and resumable behavior.\n\n## Scope\n\n* `src/cli/adopt-smartdocs.ts` — implement `migrateSmartDocs(plan: AdoptionPlan): Promise<void>`\n* `src/cli/init.ts` — wire the step into Phase C after `.polaris/` scaffold creation\n* For each pending adoption step with `category: \"smartdocs-migrate\"`:\n  1. Attempt `git mv <source_path> smartdocs/raw/<basename>`\n  2. Fall back to `fs.rename()` if `git mv` fails and log the fallback\n  3. Mark the step as completed in `.polaris/adoption-plan.json`\n* Write provenance entries to `.polaris/adoption-provenance.json`\n* Never move:\n  * root `README.md`\n  * `CHANGELOG.md`, `LICENSE`, `CONTRIBUTING.md`\n  * files in `test/`, `fixtures/`, `__mocks__/`\n  * files already under `smartdocs/`\n  * generated documentation output\n* On re-run, skip steps already marked completed and do not re-move files that were already moved\n\n## Acceptance Criteria\n\n* Pending SmartDocs migration steps move candidate files into `smartdocs/raw/`.\n* Provenance records are written for migrated files.\n* Excluded files are not moved.\n* Re-runs skip completed migration steps without re-moving files.\n\n## Validation\n\n* `npm run build`\n* `npm test`\n\n## Ordering\n\nDepends on approval gating from [POL-261](https://linear.app/lsctech/issue/POL-261/impl-add-user-approval-gate-to-polaris-init-adopt) because migration runs only after approval.\n\n## Non-goals\n\n* Do not move excluded repository files even if they are detected as candidates.\n* Do not silently lose history or provenance during migration.\n* Do not re-run completed migration steps on resume.\n\n## Scope\nTouch only:\n- `src/cli/adopt-smartdocs.ts` — implement `migrateSmartDocs(plan: AdoptionPlan): Promise<void>`\n- `src/cli/init.ts` — wire the step into Phase C after `.polaris/` scaffold creation\n- For each pending adoption step with `category: \"smartdocs-migrate\"`:\n- Write provenance entries to `.polaris/adoption-provenance.json`\n- Never move:\n- root `README.md`\n- `CHANGELOG.md`, `LICENSE`, `CONTRIBUTING.md`\n- files in `test/`, `fixtures/`, `__mocks__/`\n- files already under `smartdocs/`\n- generated documentation output\n- On re-run, skip steps already marked completed and do not re-move files that were already moved\n\n## Acceptance Criteria\n\n## Validation\n- `npm run build`\n- `npm test`\n\n## Commit\n[POL-263] POL-263\n\n## Governance\n- Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-263\" from open_children to completed_children.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-263\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-263\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-263\".",
      "Implement POL-263: \"POL-263\".",
      "Issue description:\n## Objective\n\nImplement the SmartDocs migration step in `polaris init --adopt` so discovered SmartDocs candidates are moved into `smartdocs/raw/` with provenance recorded.\n\n## Context\n\nThis is Phase C, Step C2 of the adoption workflow. The migration must preserve history when possible, avoid moving excluded files, and remain resumable so repeated runs do not re-move already completed steps.\n\n## Goal\n\nCreate `migrateSmartDocs(plan: AdoptionPlan): Promise<void>` and execute pending SmartDocs migration steps safely, with provenance tracking and resumable behavior.\n\n## Scope\n\n* `src/cli/adopt-smartdocs.ts` — implement `migrateSmartDocs(plan: AdoptionPlan): Promise<void>`\n* `src/cli/init.ts` — wire the step into Phase C after `.polaris/` scaffold creation\n* For each pending adoption step with `category: \"smartdocs-migrate\"`:\n  1. Attempt `git mv <source_path> smartdocs/raw/<basename>`\n  2. Fall back to `fs.rename()` if `git mv` fails and log the fallback\n  3. Mark the step as completed in `.polaris/adoption-plan.json`\n* Write provenance entries to `.polaris/adoption-provenance.json`\n* Never move:\n  * root `README.md`\n  * `CHANGELOG.md`, `LICENSE`, `CONTRIBUTING.md`\n  * files in `test/`, `fixtures/`, `__mocks__/`\n  * files already under `smartdocs/`\n  * generated documentation output\n* On re-run, skip steps already marked completed and do not re-move files that were already moved\n\n## Acceptance Criteria\n\n* Pending SmartDocs migration steps move candidate files into `smartdocs/raw/`.\n* Provenance records are written for migrated files.\n* Excluded files are not moved.\n* Re-runs skip completed migration steps without re-moving files.\n\n## Validation\n\n* `npm run build`\n* `npm test`\n\n## Ordering\n\nDepends on approval gating from [POL-261](https://linear.app/lsctech/issue/POL-261/impl-add-user-approval-gate-to-polaris-init-adopt) because migration runs only after approval.\n\n## Non-goals\n\n* Do not move excluded repository files even if they are detected as candidates.\n* Do not silently lose history or provenance during migration.\n* Do not re-run completed migration steps on resume.",
      "Requirements:",
      "   1. Pending SmartDocs migration steps move candidate files into `smartdocs/raw/`.",
      "   2. Provenance records are written for migrated files.",
      "   3. Excluded files are not moved.",
      "   4. Re-runs skip completed migration steps without re-moving files.",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-263] POL-263.",
      "Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-263\" from open_children to completed_children.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action).",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "`src/cli/adopt-smartdocs.ts` — implement `migrateSmartDocs(plan: AdoptionPlan): Promise<void>`",
      "`src/cli/init.ts` — wire the step into Phase C after `.polaris/` scaffold creation",
      "For each pending adoption step with `category: \"smartdocs-migrate\"`:",
      "Write provenance entries to `.polaris/adoption-provenance.json`",
      "Never move:",
      "root `README.md`",
      "`CHANGELOG.md`, `LICENSE`, `CONTRIBUTING.md`",
      "files in `test/`, `fixtures/`, `__mocks__/`",
      "files already under `smartdocs/`",
      "generated documentation output",
      "On re-run, skip steps already marked completed and do not re-move files that were already moved"
    ],
    "validation_commands": [
      "`npm run build`",
      "`npm test`"
    ],
    "issue_context": {
      "id": "POL-263",
      "title": "POL-263",
      "key_requirements": [],
      "body": "## Objective\n\nImplement the SmartDocs migration step in `polaris init --adopt` so discovered SmartDocs candidates are moved into `smartdocs/raw/` with provenance recorded.\n\n## Context\n\nThis is Phase C, Step C2 of the adoption workflow. The migration must preserve history when possible, avoid moving excluded files, and remain resumable so repeated runs do not re-move already completed steps.\n\n## Goal\n\nCreate `migrateSmartDocs(plan: AdoptionPlan): Promise<void>` and execute pending SmartDocs migration steps safely, with provenance tracking and resumable behavior.\n\n## Scope\n\n* `src/cli/adopt-smartdocs.ts` — implement `migrateSmartDocs(plan: AdoptionPlan): Promise<void>`\n* `src/cli/init.ts` — wire the step into Phase C after `.polaris/` scaffold creation\n* For each pending adoption step with `category: \"smartdocs-migrate\"`:\n  1. Attempt `git mv <source_path> smartdocs/raw/<basename>`\n  2. Fall back to `fs.rename()` if `git mv` fails and log the fallback\n  3. Mark the step as completed in `.polaris/adoption-plan.json`\n* Write provenance entries to `.polaris/adoption-provenance.json`\n* Never move:\n  * root `README.md`\n  * `CHANGELOG.md`, `LICENSE`, `CONTRIBUTING.md`\n  * files in `test/`, `fixtures/`, `__mocks__/`\n  * files already under `smartdocs/`\n  * generated documentation output\n* On re-run, skip steps already marked completed and do not re-move files that were already moved\n\n## Acceptance Criteria\n\n* Pending SmartDocs migration steps move candidate files into `smartdocs/raw/`.\n* Provenance records are written for migrated files.\n* Excluded files are not moved.\n* Re-runs skip completed migration steps without re-moving files.\n\n## Validation\n\n* `npm run build`\n* `npm test`\n\n## Ordering\n\nDepends on approval gating from [POL-261](https://linear.app/lsctech/issue/POL-261/impl-add-user-approval-gate-to-polaris-init-adopt) because migration runs only after approval.\n\n## Non-goals\n\n* Do not move excluded repository files even if they are detected as candidates.\n* Do not silently lose history or provenance during migration.\n* Do not re-run completed migration steps on resume."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 7075,
    "estimated_tokens": 1769
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-268/results/POL-263-5c6d3559-b602-4386-8339-7545c13fea20.json"
  },
  "context": {
    "branch": "main",
    "worker_role": "impl"
  }
}