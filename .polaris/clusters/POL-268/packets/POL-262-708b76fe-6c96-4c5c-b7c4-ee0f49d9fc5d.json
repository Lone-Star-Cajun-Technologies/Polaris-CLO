{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-268-2026-06-02-001",
  "cluster_id": "POL-268",
  "active_child": "POL-262",
  "validation_waiver": true,
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-262 — POL-262\nWorktree: .\nBranch: main\n\n## Goal\n## Objective\n\nWrite `polaris.config.json` with dispatch-safe defaults as the first write in `polaris init --adopt`, before the repository scan runs.\n\n## Context\n\nThis is Phase A, Step A1 of the adoption flow and the only permitted write in Phase A. The adoption session needs a locked configuration surface before scanning so dispatch stays safe even when an existing repository already contains partial Polaris config.\n\nThis step must integrate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) without re-defining provider roles here.\n\n## Goal\n\nAdd an adoption config lock that merges with existing config but forces the required locked keys to win for the adoption session, while remaining idempotent on resume.\n\n## Scope\n\n* `src/cli/init.ts` — add `writeAdoptionConfigLock()` before `scanRepo()`\n* `src/config/loader.ts` — verify deep-merge behavior for locked keys\n* Required locked config values:\n\n```json\n{\n  \"execution\": {\n    \"rotation\": [],\n    \"allowCrossAgentFallback\": false,\n    \"adapter\": \"terminal-cli\"\n  },\n  \"orchestration\": {\n    \"mode\": \"supervised\"\n  }\n}\n```\n\n* Merge with an existing `polaris.config.json` when present, but the locked keys always win for the adoption session\n* If the locked values are already present, skip the write and log `Provider config already locked — skipping.`\n* Coordinate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) for the provider role hook point, but do not define provider roles here\n\n## Acceptance Criteria\n\n* `polaris.config.json` is written before any scan or plan generation.\n* Locked keys cannot be overridden by a pre-existing partial config during the adoption session.\n* The behavior is idempotent on resume.\n\n## Validation\n\n* `npm run build`\n* `npm test`\n\n## Ordering\n\nDepends on repo state detection from <issue id=\"c0f3ada5-9835-48e3-e546-00a9b75cd162\">[POL-258](https://linear.app/lsctech/issue/POL-258/impl-add-repo-state-detection-to-polaris-init)</issue> and must run before the repo scan step.\n\n## Non-goals\n\n* Do not define provider role policy in this issue.\n* Do not expand this step into scanner or plan-generation behavior.\n* Do not allow pre-existing config to override the locked adoption-session safety keys.\n\n## Scope\nTouch only:\n- `src/cli/init.ts` — add `writeAdoptionConfigLock()` before `scanRepo()`\n- `src/config/loader.ts` — verify deep-merge behavior for locked keys\n- Required locked config values:\n- Merge with an existing `polaris.config.json` when present, but the locked keys always win for the adoption session\n- If the locked values are already present, skip the write and log `Provider config already locked — skipping.`\n- Coordinate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) for the provider role hook point, but do not define provider roles here\n\n## Acceptance Criteria\n\n## Validation\n- `npm run build`\n- `npm test`\n\n## Commit\n[POL-262] POL-262\n\n## Governance\n- Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-262\" from open_children to completed_children.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-262\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-262\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-262\".",
      "Implement POL-262: \"POL-262\".",
      "Issue description:\n## Objective\n\nWrite `polaris.config.json` with dispatch-safe defaults as the first write in `polaris init --adopt`, before the repository scan runs.\n\n## Context\n\nThis is Phase A, Step A1 of the adoption flow and the only permitted write in Phase A. The adoption session needs a locked configuration surface before scanning so dispatch stays safe even when an existing repository already contains partial Polaris config.\n\nThis step must integrate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) without re-defining provider roles here.\n\n## Goal\n\nAdd an adoption config lock that merges with existing config but forces the required locked keys to win for the adoption session, while remaining idempotent on resume.\n\n## Scope\n\n* `src/cli/init.ts` — add `writeAdoptionConfigLock()` before `scanRepo()`\n* `src/config/loader.ts` — verify deep-merge behavior for locked keys\n* Required locked config values:\n\n```json\n{\n  \"execution\": {\n    \"rotation\": [],\n    \"allowCrossAgentFallback\": false,\n    \"adapter\": \"terminal-cli\"\n  },\n  \"orchestration\": {\n    \"mode\": \"supervised\"\n  }\n}\n```\n\n* Merge with an existing `polaris.config.json` when present, but the locked keys always win for the adoption session\n* If the locked values are already present, skip the write and log `Provider config already locked — skipping.`\n* Coordinate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) for the provider role hook point, but do not define provider roles here\n\n## Acceptance Criteria\n\n* `polaris.config.json` is written before any scan or plan generation.\n* Locked keys cannot be overridden by a pre-existing partial config during the adoption session.\n* The behavior is idempotent on resume.\n\n## Validation\n\n* `npm run build`\n* `npm test`\n\n## Ordering\n\nDepends on repo state detection from <issue id=\"c0f3ada5-9835-48e3-e546-00a9b75cd162\">[POL-258](https://linear.app/lsctech/issue/POL-258/impl-add-repo-state-detection-to-polaris-init)</issue> and must run before the repo scan step.\n\n## Non-goals\n\n* Do not define provider role policy in this issue.\n* Do not expand this step into scanner or plan-generation behavior.\n* Do not allow pre-existing config to override the locked adoption-session safety keys.",
      "Requirements:",
      "   1. `polaris.config.json` is written before any scan or plan generation.",
      "   2. Locked keys cannot be overridden by a pre-existing partial config during the adoption session.",
      "   3. The behavior is idempotent on resume.",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-262] POL-262.",
      "Update /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json: move \"POL-262\" from open_children to completed_children.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-268-2026-06-02-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action).",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "`src/cli/init.ts` — add `writeAdoptionConfigLock()` before `scanRepo()`",
      "`src/config/loader.ts` — verify deep-merge behavior for locked keys",
      "Required locked config values:",
      "Merge with an existing `polaris.config.json` when present, but the locked keys always win for the adoption session",
      "If the locked values are already present, skip the write and log `Provider config already locked — skipping.`",
      "Coordinate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) for the provider role hook point, but do not define provider roles here"
    ],
    "validation_commands": [
      "`npm run build`",
      "`npm test`"
    ],
    "issue_context": {
      "id": "POL-262",
      "title": "POL-262",
      "key_requirements": [],
      "body": "## Objective\n\nWrite `polaris.config.json` with dispatch-safe defaults as the first write in `polaris init --adopt`, before the repository scan runs.\n\n## Context\n\nThis is Phase A, Step A1 of the adoption flow and the only permitted write in Phase A. The adoption session needs a locked configuration surface before scanning so dispatch stays safe even when an existing repository already contains partial Polaris config.\n\nThis step must integrate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) without re-defining provider roles here.\n\n## Goal\n\nAdd an adoption config lock that merges with existing config but forces the required locked keys to win for the adoption session, while remaining idempotent on resume.\n\n## Scope\n\n* `src/cli/init.ts` — add `writeAdoptionConfigLock()` before `scanRepo()`\n* `src/config/loader.ts` — verify deep-merge behavior for locked keys\n* Required locked config values:\n\n```json\n{\n  \"execution\": {\n    \"rotation\": [],\n    \"allowCrossAgentFallback\": false,\n    \"adapter\": \"terminal-cli\"\n  },\n  \"orchestration\": {\n    \"mode\": \"supervised\"\n  }\n}\n```\n\n* Merge with an existing `polaris.config.json` when present, but the locked keys always win for the adoption session\n* If the locked values are already present, skip the write and log `Provider config already locked — skipping.`\n* Coordinate with [POL-256](https://linear.app/lsctech/issue/POL-256/analyze-define-provider-role-config-fallback-and-dispatch-governance) for the provider role hook point, but do not define provider roles here\n\n## Acceptance Criteria\n\n* `polaris.config.json` is written before any scan or plan generation.\n* Locked keys cannot be overridden by a pre-existing partial config during the adoption session.\n* The behavior is idempotent on resume.\n\n## Validation\n\n* `npm run build`\n* `npm test`\n\n## Ordering\n\nDepends on repo state detection from <issue id=\"c0f3ada5-9835-48e3-e546-00a9b75cd162\">[POL-258](https://linear.app/lsctech/issue/POL-258/impl-add-repo-state-detection-to-polaris-init)</issue> and must run before the repo scan step.\n\n## Non-goals\n\n* Do not define provider role policy in this issue.\n* Do not expand this step into scanner or plan-generation behavior.\n* Do not allow pre-existing config to override the locked adoption-session safety keys."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 7323,
    "estimated_tokens": 1831
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-268/results/POL-262-708b76fe-6c96-4c5c-b7c4-ee0f49d9fc5d.json"
  },
  "context": {
    "branch": "main",
    "worker_role": "impl"
  }
}