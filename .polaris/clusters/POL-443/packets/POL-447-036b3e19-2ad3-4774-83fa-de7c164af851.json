{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-443-2026-07-03-001",
  "cluster_id": "POL-443",
  "active_child": "POL-447",
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-447 — Add two-tier link staleness checking (permissive raw/, strict candidate+active)\nWorktree: .\nBranch: pol-443-delivery\n\n## Goal\n## Objective\n\nFlag broken markdown cross-links in `smartdocs/doctrine/candidate/` and `smartdocs/doctrine/active/` (and the `specs/` equivalents) as a `stale-assumption` signal, while leaving `raw/` fully permissive per OKF's tolerate-broken-links rule.\n\n## Context\n\nSpec: `smartdocs/raw/smartdocs-2-0-architecture.md` §7.7. OKF §5.3 requires consumers to tolerate broken links everywhere — fine for the traversal layer, but a broken link from an *active* doctrine doc to a deleted concept is a real staleness signal Polaris's governance layer should not silently ignore. `docs-authority-model.md` §3.5 already defines a `stale-assumption` conflict signal for a different case (references to old API/structure); this child adds a second producer of that same signal for broken cross-links.\n\n## Goal\n\nA link-checking pass that: for every markdown link in a `candidate/` or `active/` doc pointing at another `smartdocs/**/*.md` file, verifies the target exists; missing targets are reported using the existing `stale-assumption` signal shape (see `detectDoctrineConflict` / `SpecConflict` in `doctrine.ts`) rather than a new one. Links from `raw/` documents are never checked (matches OKF's permissive default).\n\n## Scope\n\n* src/smartdocs-engine/doctrine.ts\n* src/smartdocs-engine/canon-check.ts\n* src/smartdocs-engine/canon-check.test.ts\n\n## Acceptance Criteria\n\n- [ ] Link resolution implemented for `candidate/` and `active/` tiers only\n- [ ] Broken links reported via the existing `stale-assumption` conflict signal shape — no new telemetry event type invented\n- [ ] `raw/` documents are never link-checked\n- [ ] Unit tests cover: valid link, broken link in `active/` (flagged), broken link in `raw/` (not flagged), bundle-relative (`/path.md`) and relative (`./path.md`) link forms both resolved correctly\n\n## Validation\n\n* npm run build\n* npx vitest run src/smartdocs-engine/canon-check.test.ts\n\n## Ordering\n\n* None.\n\n## Non-goals\n\n* Do not build a full link graph or cross-link visualization.\n* Do not auto-fix or remove broken links — flag only.\n* Do not check links to files outside `smartdocs/` (e.g. links into `src/`) — out of scope for this signal.\n\n## Implementation Discipline\nBefore writing code, stop at the first rung that holds:\n\n1. Does this need to exist?   → no: skip it (YAGNI)\n2. Stdlib does it?            → use it\n3. Native platform feature?   → use it\n4. Installed dependency?      → use it\n5. One line?                  → one line\n6. Only then: the minimum that works\n\nLazy, not negligent: validation, error handling, security, and accessibility are never on the chopping block.\n\nWhen you defer a simplification for later, mark it inline:\n  // ponytail: <what you deferred and why>\n\n## Scope\nTouch only:\n- src/smartdocs-engine/doctrine.ts\n- src/smartdocs-engine/canon-check.ts\n- src/smartdocs-engine/canon-check.test.ts\n\n## Acceptance Criteria\n\n## Validation\n- npm run build\n- npx vitest run src/smartdocs-engine/canon-check.test.ts\n\n## Commit\n[POL-447] Add two-tier link staleness checking (permissive raw/, strict candidate+active)\n\n## Governance\n- Do NOT modify open_children or completed_children in /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json. The parent runtime (loop continue) owns state transitions.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-447\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-447\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-447\".",
      "Implement POL-447: \"Add two-tier link staleness checking (permissive raw/, strict candidate+active)\".",
      "Issue description:\n## Objective\n\nFlag broken markdown cross-links in `smartdocs/doctrine/candidate/` and `smartdocs/doctrine/active/` (and the `specs/` equivalents) as a `stale-assumption` signal, while leaving `raw/` fully permissive per OKF's tolerate-broken-links rule.\n\n## Context\n\nSpec: `smartdocs/raw/smartdocs-2-0-architecture.md` §7.7. OKF §5.3 requires consumers to tolerate broken links everywhere — fine for the traversal layer, but a broken link from an *active* doctrine doc to a deleted concept is a real staleness signal Polaris's governance layer should not silently ignore. `docs-authority-model.md` §3.5 already defines a `stale-assumption` conflict signal for a different case (references to old API/structure); this child adds a second producer of that same signal for broken cross-links.\n\n## Goal\n\nA link-checking pass that: for every markdown link in a `candidate/` or `active/` doc pointing at another `smartdocs/**/*.md` file, verifies the target exists; missing targets are reported using the existing `stale-assumption` signal shape (see `detectDoctrineConflict` / `SpecConflict` in `doctrine.ts`) rather than a new one. Links from `raw/` documents are never checked (matches OKF's permissive default).\n\n## Scope\n\n* src/smartdocs-engine/doctrine.ts\n* src/smartdocs-engine/canon-check.ts\n* src/smartdocs-engine/canon-check.test.ts\n\n## Acceptance Criteria\n\n- [ ] Link resolution implemented for `candidate/` and `active/` tiers only\n- [ ] Broken links reported via the existing `stale-assumption` conflict signal shape — no new telemetry event type invented\n- [ ] `raw/` documents are never link-checked\n- [ ] Unit tests cover: valid link, broken link in `active/` (flagged), broken link in `raw/` (not flagged), bundle-relative (`/path.md`) and relative (`./path.md`) link forms both resolved correctly\n\n## Validation\n\n* npm run build\n* npx vitest run src/smartdocs-engine/canon-check.test.ts\n\n## Ordering\n\n* None.\n\n## Non-goals\n\n* Do not build a full link graph or cross-link visualization.\n* Do not auto-fix or remove broken links — flag only.\n* Do not check links to files outside `smartdocs/` (e.g. links into `src/`) — out of scope for this signal.",
      "Requirements:",
      "   1. [ ] Link resolution implemented for `candidate/` and `active/` tiers only",
      "   2. [ ] Broken links reported via the existing `stale-assumption` conflict signal shape — no new telemetry event type invented",
      "   3. [ ] `raw/` documents are never link-checked",
      "   4. [ ] Unit tests cover: valid link, broken link in `active/` (flagged), broken link in `raw/` (not flagged), bundle-relative (`/path.md`) and relative (`./path.md`) link forms both resolved correctly",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-447] Add two-tier link staleness checking (permissive raw/, strict candidate+active).",
      "Do NOT modify open_children or completed_children in /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json. The parent runtime (loop continue) owns that state transition.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action). next_recommended_action MUST be exactly \"continue\" on success, \"stop\" on unresolvable blocker, or \"investigate\" if manual review is needed.",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "src/smartdocs-engine/doctrine.ts",
      "src/smartdocs-engine/canon-check.ts",
      "src/smartdocs-engine/canon-check.test.ts"
    ],
    "validation_commands": [
      "npm run build",
      "npx vitest run src/smartdocs-engine/canon-check.test.ts"
    ],
    "issue_context": {
      "id": "POL-447",
      "title": "Add two-tier link staleness checking (permissive raw/, strict candidate+active)",
      "key_requirements": [],
      "body": "## Objective\n\nFlag broken markdown cross-links in `smartdocs/doctrine/candidate/` and `smartdocs/doctrine/active/` (and the `specs/` equivalents) as a `stale-assumption` signal, while leaving `raw/` fully permissive per OKF's tolerate-broken-links rule.\n\n## Context\n\nSpec: `smartdocs/raw/smartdocs-2-0-architecture.md` §7.7. OKF §5.3 requires consumers to tolerate broken links everywhere — fine for the traversal layer, but a broken link from an *active* doctrine doc to a deleted concept is a real staleness signal Polaris's governance layer should not silently ignore. `docs-authority-model.md` §3.5 already defines a `stale-assumption` conflict signal for a different case (references to old API/structure); this child adds a second producer of that same signal for broken cross-links.\n\n## Goal\n\nA link-checking pass that: for every markdown link in a `candidate/` or `active/` doc pointing at another `smartdocs/**/*.md` file, verifies the target exists; missing targets are reported using the existing `stale-assumption` signal shape (see `detectDoctrineConflict` / `SpecConflict` in `doctrine.ts`) rather than a new one. Links from `raw/` documents are never checked (matches OKF's permissive default).\n\n## Scope\n\n* src/smartdocs-engine/doctrine.ts\n* src/smartdocs-engine/canon-check.ts\n* src/smartdocs-engine/canon-check.test.ts\n\n## Acceptance Criteria\n\n- [ ] Link resolution implemented for `candidate/` and `active/` tiers only\n- [ ] Broken links reported via the existing `stale-assumption` conflict signal shape — no new telemetry event type invented\n- [ ] `raw/` documents are never link-checked\n- [ ] Unit tests cover: valid link, broken link in `active/` (flagged), broken link in `raw/` (not flagged), bundle-relative (`/path.md`) and relative (`./path.md`) link forms both resolved correctly\n\n## Validation\n\n* npm run build\n* npx vitest run src/smartdocs-engine/canon-check.test.ts\n\n## Ordering\n\n* None.\n\n## Non-goals\n\n* Do not build a full link graph or cross-link visualization.\n* Do not auto-fix or remove broken links — flag only.\n* Do not check links to files outside `smartdocs/` (e.g. links into `src/`) — out of scope for this signal."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 7431,
    "estimated_tokens": 1858
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "prohibited_write_paths": [
    ".taskchain_artifacts/",
    ".polaris/clusters/",
    ".polaris/runs/",
    "**/telemetry.jsonl"
  ],
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-443/results/POL-447-036b3e19-2ad3-4774-83fa-de7c164af851.json",
    "result_required_fields": {
      "run_id": "polaris-run-pol-443-2026-07-03-001",
      "cluster_id": "POL-443",
      "child_id": "<child_id>",
      "status": "<status>",
      "commit": "<commit>",
      "validation": "<validation>",
      "next_recommended_action": "<next_recommended_action>"
    }
  },
  "context": {
    "branch": "pol-443-delivery",
    "worker_role": "impl"
  }
}