{
  "schema_version": "2.1",
  "worker_role": "impl",
  "run_id": "polaris-run-pol-443-2026-07-03-001",
  "cluster_id": "POL-443",
  "active_child": "POL-444",
  "state_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json",
  "telemetry_file": "/Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl",
  "instructions": {
    "primary_goal": "Issue: POL-444 — Generate bundle-root and per-directory index.md files for smartdocs/\nWorktree: .\nBranch: pol-443-delivery\n\n## Goal\n## Objective\n\nGenerate a canonical bundle-root `smartdocs/index.md` and per-directory `index.md` files, giving SmartDocs the OKF v0.1 §6 progressive-disclosure entry point it currently lacks.\n\n## Context\n\nSpec: `smartdocs/raw/smartdocs-2-0-architecture.md` §7.2 (canonical entry point), §7.3 (breadcrumb traversal), §9. Currently each of ~45+ folders has its own `POLARIS.md` front door but there is no single repo-wide start file. `index.md` is an OKF-reserved filename with no frontmatter (except an optional `okf_version` key on the bundle-root file only).\n\n## Goal\n\nA generator, wired into the same write path `seed-instructions.ts` already uses, that produces:\n\n1. One bundle-root `smartdocs/index.md` with `okf_version: \"0.1\"` frontmatter, sectioned by governance/routes, linking into `doctrine/active/`, `specs/active/`, and route POLARIS.md files (via the atlas).\n2. A directory-level `index.md` for every `smartdocs/**` subdirectory lacking one, listing that directory's concept files with their frontmatter `description` (or title/filename fallback).\n   Neither generator overwrites a human-edited `index.md` (reuse the existing `DRAFT_MARKER` skip-if-present pattern from `seedInstructions`/`seedSummary`).\n\n## Scope\n\n* src/smartdocs-engine/seed-instructions.ts\n* src/smartdocs-engine/index.ts\n* src/smartdocs-engine/seed-instructions.test.ts\n\n## Acceptance Criteria\n\n- [ ] Bundle-root `smartdocs/index.md` generator implemented, reuses `DRAFT_MARKER` protection for human edits\n- [ ] Per-directory `index.md` generator implemented, same protection\n- [ ] Both wired into the same CLI/skill path as existing `seed-instructions`/`seed-summary` (no new dispatch mechanism)\n- [ ] Generated files pass OKF §6 shape: no frontmatter on directory-level `index.md`; bundle-root `index.md` frontmatter contains only `okf_version`\n- [ ] Unit tests cover: fresh generation, skip-if-human-edited, regeneration over an existing draft\n\n## Validation\n\n* npm run build\n* npx vitest run src/smartdocs-engine/seed-instructions.test.ts\n\n## Ordering\n\n* None.\n\n## Non-goals\n\n* Do not replace or modify existing route-local `POLARIS.md` files — they remain authoritative.\n* Do not generate `index.md` for `raw/` drop-zone directories (pre-classification content is not yet part of the addressable bundle).\n\n## Implementation Discipline\nBefore writing code, stop at the first rung that holds:\n\n1. Does this need to exist?   → no: skip it (YAGNI)\n2. Stdlib does it?            → use it\n3. Native platform feature?   → use it\n4. Installed dependency?      → use it\n5. One line?                  → one line\n6. Only then: the minimum that works\n\nLazy, not negligent: validation, error handling, security, and accessibility are never on the chopping block.\n\nWhen you defer a simplification for later, mark it inline:\n  // ponytail: <what you deferred and why>\n\n## Scope\nTouch only:\n- src/smartdocs-engine/seed-instructions.ts\n- src/smartdocs-engine/index.ts\n- src/smartdocs-engine/seed-instructions.test.ts\n\n## Acceptance Criteria\n\n## Validation\n- npm run build\n- npx vitest run src/smartdocs-engine/seed-instructions.test.ts\n\n## Commit\n[POL-444] Generate bundle-root and per-directory index.md files for smartdocs/\n\n## Governance\n- Do NOT modify open_children or completed_children in /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json. The parent runtime (loop continue) owns state transitions.\n- Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl.\n\n## Worker Progress Telemetry (MANDATORY)\nThe parent CANNOT see your work. You MUST emit heartbeat events so the parent knows you are alive and making progress.\n\nAfter EVERY step, append a JSONL event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl:\n```jsonl\n{\"event\":\"worker-heartbeat\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-444\",\"step_cursor\":\"<current step>\",\"timestamp\":\"<ISO8601>\",\"progress_pct\":<0-100>,\"files_changed\":<count>,\"current_file\":\"<file being edited>\"}\n```\n\nRequired heartbeat points:\n  1. Immediately after reading this packet (step_cursor: \"start\")\n  2. After verifying state file (step_cursor: \"verify\")\n  3. After EACH file edit (step_cursor: \"implement\")\n  4. After running validation (step_cursor: \"validate\")\n  5. After creating commit (step_cursor: \"commit\")\n  6. Before terminating (step_cursor: \"complete\" or \"failed\")\n\nIf heartbeats stop, the parent will assume you crashed and may dispatch a replacement worker.\n\n## Approval Escalation (when uncertain)\nYou have a sealed packet defining your exact scope, allowed files, and validation commands. Use this to decide:\n\nRule 1 - Auto-approve WITHIN scope (normal work):\n  - If the operation touches files within `instructions.allowed_scope` → Just do it\n  - If the command is in `instructions.validation_commands` → Just run it\n  - Low risk, expected work → No need to ask, emit heartbeat with step_cursor: \"implement\"\n\nRule 2 - ESCALATE out-of-scope or high-risk:\n  - File outside allowed_scope → Emit worker-blocked with approval_type: \"out-of-scope\"\n  - Destructive operation (rm -rf, major refactor) → Emit worker-blocked with approval_type: \"destructive\"\n  - External API calls with cost → Emit worker-blocked with approval_type: \"cost\"\n  - Changing secrets/.env → Emit worker-blocked with approval_type: \"security\"\n  - Unclear requirements → Emit worker-blocked with approval_type: \"ambiguous\"\n\nTo escalate, emit IMMEDIATELY to telemetry:\n```jsonl\n{\"event\":\"worker-blocked\",\"run_id\":\"<from packet>\",\"child_id\":\"POL-444\",\"reason\":\"needs-approval\",\"approval_type\":\"<out-of-scope|destructive|cost|security|ambiguous>\",\"description\":\"<specifically what needs approval>\",\"blocker_id\":\"<unique-id>\",\"timestamp\":\"<ISO8601>\"}\n```\n\nThen: Continue heartbeats every 30s with step_cursor: \"blocked-awaiting-approval\"\nWait for parent response. If no response in 5 minutes → TERMINATE with approval-timeout.\n\nRemember: Your packet scope is your authority. Within scope = proceed. Outside scope = escalate.\n\n## Route Cognition Delta\nAfter implementation, apply route-local cognition delta — only if something materially changed.\n\n**POLARIS.md** — update only when ALL of the following are true:\n  - You touched a file in that folder (not a distant folder).\n  - The change materially affects: folder responsibilities, commands/workflows,\n    execution constraints, ownership/routing, or operational behavior.\n  - The current POLARIS.md content is actually wrong or incomplete as a result.\n  DO NOT update POLARIS.md for: formatting fixes, comment changes, tiny refactors,\n  internal implementation details, or any change that leaves the operational guidance\n  still accurate. When in doubt, do not update.\n\n**SUMMARY.md** — update only when: linked docs/specs changed, canon relationships\n  changed, architecture meaning changed, or doctrine/spec linkage changed.\n  SUMMARY.md is informational only. Do not create it if missing — that is a hint for\n  operators, not a task for workers. Keep it short. Never add operational doctrine.\n\n**Never** scan unrelated folders. **Never** regenerate all route docs.\nIf no material change occurred, skip this section entirely.\n\n## Report Back\n- files changed\n- validation results\n- commit hash\n- blockers\n\nTERMINATE SESSION IMMEDIATELY. Do not select or execute the next child.",
    "steps": [
      "Verify: read /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json and confirm active_child === \"POL-444\".",
      "Implement POL-444: \"Generate bundle-root and per-directory index.md files for smartdocs/\".",
      "Issue description:\n## Objective\n\nGenerate a canonical bundle-root `smartdocs/index.md` and per-directory `index.md` files, giving SmartDocs the OKF v0.1 §6 progressive-disclosure entry point it currently lacks.\n\n## Context\n\nSpec: `smartdocs/raw/smartdocs-2-0-architecture.md` §7.2 (canonical entry point), §7.3 (breadcrumb traversal), §9. Currently each of ~45+ folders has its own `POLARIS.md` front door but there is no single repo-wide start file. `index.md` is an OKF-reserved filename with no frontmatter (except an optional `okf_version` key on the bundle-root file only).\n\n## Goal\n\nA generator, wired into the same write path `seed-instructions.ts` already uses, that produces:\n\n1. One bundle-root `smartdocs/index.md` with `okf_version: \"0.1\"` frontmatter, sectioned by governance/routes, linking into `doctrine/active/`, `specs/active/`, and route POLARIS.md files (via the atlas).\n2. A directory-level `index.md` for every `smartdocs/**` subdirectory lacking one, listing that directory's concept files with their frontmatter `description` (or title/filename fallback).\n   Neither generator overwrites a human-edited `index.md` (reuse the existing `DRAFT_MARKER` skip-if-present pattern from `seedInstructions`/`seedSummary`).\n\n## Scope\n\n* src/smartdocs-engine/seed-instructions.ts\n* src/smartdocs-engine/index.ts\n* src/smartdocs-engine/seed-instructions.test.ts\n\n## Acceptance Criteria\n\n- [ ] Bundle-root `smartdocs/index.md` generator implemented, reuses `DRAFT_MARKER` protection for human edits\n- [ ] Per-directory `index.md` generator implemented, same protection\n- [ ] Both wired into the same CLI/skill path as existing `seed-instructions`/`seed-summary` (no new dispatch mechanism)\n- [ ] Generated files pass OKF §6 shape: no frontmatter on directory-level `index.md`; bundle-root `index.md` frontmatter contains only `okf_version`\n- [ ] Unit tests cover: fresh generation, skip-if-human-edited, regeneration over an existing draft\n\n## Validation\n\n* npm run build\n* npx vitest run src/smartdocs-engine/seed-instructions.test.ts\n\n## Ordering\n\n* None.\n\n## Non-goals\n\n* Do not replace or modify existing route-local `POLARIS.md` files — they remain authoritative.\n* Do not generate `index.md` for `raw/` drop-zone directories (pre-classification content is not yet part of the addressable bundle).",
      "Requirements:",
      "   1. [ ] Bundle-root `smartdocs/index.md` generator implemented, reuses `DRAFT_MARKER` protection for human edits",
      "   2. [ ] Per-directory `index.md` generator implemented, same protection",
      "   3. [ ] Both wired into the same CLI/skill path as existing `seed-instructions`/`seed-summary` (no new dispatch mechanism)",
      "   4. [ ] Generated files pass OKF §6 shape: no frontmatter on directory-level `index.md`; bundle-root `index.md` frontmatter contains only `okf_version`",
      "   5. [ ] Unit tests cover: fresh generation, skip-if-human-edited, regeneration over an existing draft",
      "Run validation commands and confirm all pass.",
      "Create exactly ONE git commit: [POL-444] Generate bundle-root and per-directory index.md files for smartdocs/.",
      "Do NOT modify open_children or completed_children in /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/current-state.json. The parent runtime (loop continue) owns that state transition.",
      "Append a telemetry event to /Users/lsctech/Developer/Polaris/.taskchain_artifacts/polaris-run/runs/polaris-run-pol-443-2026-07-03-001/telemetry.jsonl.",
      "Write compact return JSON to stdout (fields: child_id, status, commit, validation, next_recommended_action). next_recommended_action MUST be exactly \"continue\" on success, \"stop\" on unresolvable blocker, or \"investigate\" if manual review is needed.",
      "TERMINATE SESSION IMMEDIATELY. Do not select or execute the next child."
    ],
    "allowed_scope": [
      "src/smartdocs-engine/seed-instructions.ts",
      "src/smartdocs-engine/index.ts",
      "src/smartdocs-engine/seed-instructions.test.ts"
    ],
    "validation_commands": [
      "npm run build",
      "npx vitest run src/smartdocs-engine/seed-instructions.test.ts"
    ],
    "issue_context": {
      "id": "POL-444",
      "title": "Generate bundle-root and per-directory index.md files for smartdocs/",
      "key_requirements": [],
      "body": "## Objective\n\nGenerate a canonical bundle-root `smartdocs/index.md` and per-directory `index.md` files, giving SmartDocs the OKF v0.1 §6 progressive-disclosure entry point it currently lacks.\n\n## Context\n\nSpec: `smartdocs/raw/smartdocs-2-0-architecture.md` §7.2 (canonical entry point), §7.3 (breadcrumb traversal), §9. Currently each of ~45+ folders has its own `POLARIS.md` front door but there is no single repo-wide start file. `index.md` is an OKF-reserved filename with no frontmatter (except an optional `okf_version` key on the bundle-root file only).\n\n## Goal\n\nA generator, wired into the same write path `seed-instructions.ts` already uses, that produces:\n\n1. One bundle-root `smartdocs/index.md` with `okf_version: \"0.1\"` frontmatter, sectioned by governance/routes, linking into `doctrine/active/`, `specs/active/`, and route POLARIS.md files (via the atlas).\n2. A directory-level `index.md` for every `smartdocs/**` subdirectory lacking one, listing that directory's concept files with their frontmatter `description` (or title/filename fallback).\n   Neither generator overwrites a human-edited `index.md` (reuse the existing `DRAFT_MARKER` skip-if-present pattern from `seedInstructions`/`seedSummary`).\n\n## Scope\n\n* src/smartdocs-engine/seed-instructions.ts\n* src/smartdocs-engine/index.ts\n* src/smartdocs-engine/seed-instructions.test.ts\n\n## Acceptance Criteria\n\n- [ ] Bundle-root `smartdocs/index.md` generator implemented, reuses `DRAFT_MARKER` protection for human edits\n- [ ] Per-directory `index.md` generator implemented, same protection\n- [ ] Both wired into the same CLI/skill path as existing `seed-instructions`/`seed-summary` (no new dispatch mechanism)\n- [ ] Generated files pass OKF §6 shape: no frontmatter on directory-level `index.md`; bundle-root `index.md` frontmatter contains only `okf_version`\n- [ ] Unit tests cover: fresh generation, skip-if-human-edited, regeneration over an existing draft\n\n## Validation\n\n* npm run build\n* npx vitest run src/smartdocs-engine/seed-instructions.test.ts\n\n## Ordering\n\n* None.\n\n## Non-goals\n\n* Do not replace or modify existing route-local `POLARIS.md` files — they remain authoritative.\n* Do not generate `index.md` for `raw/` drop-zone directories (pre-classification content is not yet part of the addressable bundle)."
    }
  },
  "lifecycle": {
    "terminate_after_completion": true,
    "max_concurrent": 1,
    "cleanup_on_exit": "commit-and-exit"
  },
  "return_contract": [
    "child_id",
    "status",
    "commit",
    "validation",
    "next_recommended_action"
  ],
  "prompt_mode": "compact",
  "prompt_metrics": {
    "mode": "compact",
    "char_count": 7558,
    "estimated_tokens": 1890
  },
  "role_context": {
    "role": "worker",
    "role_authority": "implementation",
    "may_implement": true,
    "may_assign_workers": false,
    "prohibited_actions": [
      "modify-cluster-plan",
      "dispatch-children",
      "polaris-loop-dispatch",
      "polaris-loop-continue"
    ]
  },
  "prohibited_write_paths": [
    ".taskchain_artifacts/",
    ".polaris/clusters/",
    ".polaris/runs/",
    "**/telemetry.jsonl"
  ],
  "result_file_contract": {
    "result_file": "/Users/lsctech/Developer/Polaris/.polaris/clusters/POL-443/results/POL-444-295c9914-8881-4017-8fd7-dbddbad9b5c7.json",
    "result_required_fields": {
      "run_id": "polaris-run-pol-443-2026-07-03-001",
      "cluster_id": "POL-443",
      "child_id": "<child_id>",
      "status": "<status>",
      "commit": "<commit>",
      "validation": "<validation>",
      "next_recommended_action": "<next_recommended_action>"
    }
  },
  "context": {
    "branch": "pol-443-delivery",
    "worker_role": "impl"
  }
}